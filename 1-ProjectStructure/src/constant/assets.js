const imgPath = `../assets/images/`;
const iconPath = `../assets/icons/`;

export const images = {
  mainLogo: require(`${imgPath}Arabic.png`), //exmaple imports
};
export const icons = {
  rightArrow: require(`${iconPath}client.png`), //exmaple imports
};
